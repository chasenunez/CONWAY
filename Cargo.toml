[package]
name = "conway"
version = "0.1.0"
edition = "2021"
description = "Conway's Game of Life in the terminal"

[dependencies]
crossterm = "0.27"
clap = { version = "4", features = ["derive"] }
rand = "0.8"

[profile.release]
opt-level = 3
lto = "thin"
